name: Keep Render Alive

on:
  schedule:
    - cron:  "*/20 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep backend awake
        run: |
          curl -I https://conectavidas-backend.onrender.com/health || exit 0
